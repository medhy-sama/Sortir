{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(userForm) }}
    {{ form_widget(userForm) }}
    <div>{{ message }}</div>
    <button>Enregistrer</button>
    {{ form_end(userForm) }}
{% endblock %}

{% block title %}

{% endblock %}

